// Availability verification is delegated to the high-precision student-placement verifier.
// The verifier checks the actual student programme, intake, exact live role, and
// application window instead of treating generic Apply buttons as availability.
import './reliable-role-verification.mjs'
