import { useEffect, useRef, useState } from 'react'
import { supabase } from './lib/supabase'
import type { Placement, OverallPriority, AppStatus } from './lib/supabase'
import { PRIORITY_LABELS, PRIORITY_COLORS } from './lib/utils'
import { filterPlacements, normaliseApplicationStatus, sortFilteredPlacements, type SortOption, type SectorGroup, type CountryGroup } from './lib/filtering'
import { downloadExcel } from './lib/excel'
import PlacementCard from './components/PlacementCard'
import './App.css'

type FilterPriority = 'all' | OverallPriority
type View = 'opportunities' | 'applications'
const APP_STAGES: AppStatus[] = ['Saved','Applied','Assessment','Interview','Final Interview','Offer','Accepted','Rejected','Withdrawn']
const SECTORS: SectorGroup[] = ['Aerospace & Space','Defence','Motorsport','Engineering & Technology','Research & Advanced Tech']
const COUNTRIES: CountryGroup[] = ['UK','Europe','Asia','Oceania','America']

export default function App() {
  const [placements, setPlacements] = useState<Placement[]>([]); const [loading, setLoading] = useState(true); const [error, setError] = useState<string | null>(null)
  const [filterPriority, setFilterPriority] = useState<FilterPriority>('all'); const [filterSector, setFilterSector] = useState('all'); const [filterCountry, setFilterCountry] = useState('all'); const [filterStatus, setFilterStatus] = useState('all'); const [filterStage, setFilterStage] = useState('all'); const [search, setSearch] = useState(''); const [sortBy, setSortBy] = useState<SortOption>('relevance'); const [view, setView] = useState<View>('opportunities'); const [newIds, setNewIds] = useState<Set<string>>(new Set()); const [connected, setConnected] = useState(false); const prevIds = useRef<Set<string>>(new Set())
  useEffect(() => { let channel: ReturnType<typeof supabase.channel> | null = null; async function load(){const {data,error}=await supabase.from('placements').select('*').order('created_at',{ascending:false}); if(error){setError(error.message);setLoading(false);return} const loaded=data as Placement[];setPlacements(loaded);prevIds.current=new Set(loaded.map(p=>p.id));setLoading(false)} load(); channel=supabase.channel('placements-changes').on('postgres_changes',{event:'*',schema:'public',table:'placements'},payload=>{if(payload.eventType==='INSERT'){const row=payload.new as Placement;setPlacements(prev=>prev.some(p=>p.id===row.id)?prev:[row,...prev]);setNewIds(prev=>new Set(prev).add(row.id));setTimeout(()=>setNewIds(prev=>{const n=new Set(prev);n.delete(row.id);return n}),5000)}else if(payload.eventType==='UPDATE'){const row=payload.new as Placement;setPlacements(prev=>prev.map(p=>p.id===row.id?row:p))}else if(payload.eventType==='DELETE'){const row=payload.old as Placement;setPlacements(prev=>prev.filter(p=>p.id!==row.id))}}).subscribe(status=>setConnected(status==='SUBSCRIBED'));return()=>{if(channel)supabase.removeChannel(channel)}},[])
  async function updatePlacement(p: Placement){const{id,...rest}=p;setPlacements(prev=>prev.map(x=>x.id===id?p:x));const{error}=await supabase.from('placements').update(rest).eq('id',id);if(error)setError(`Could not save ${p.company}: ${error.message}`)}
  const counts={total:placements.length,open:placements.filter(p=>normaliseApplicationStatus(p.application_status)==='Open Now').length,soon:placements.filter(p=>normaliseApplicationStatus(p.application_status)==='Opening Soon').length,expected:placements.filter(p=>normaliseApplicationStatus(p.application_status)==='Expected').length,applied:placements.filter(p=>(p.app_status??'Not Applied')!=='Not Applied').length}
  const priorityCounts:Record<string,number>={all:placements.length};for(const key of Object.keys(PRIORITY_LABELS) as OverallPriority[])priorityCounts[key]=placements.filter(p=>p.overall_priority===key).length
  const filtered=sortFilteredPlacements(filterPlacements(placements,{priority:filterPriority,sector:filterSector,country:filterCountry,engineeringArea:'all',status:filterStatus,stage:filterStage,search,applicationsOnly:view==='applications'}),sortBy)
  const priorityTabs:{key:FilterPriority;label:string;color:string}[]=[{key:'all',label:'All',color:'#64748b'},{key:'APPLY_IMMEDIATELY',label:'Apply Now',color:PRIORITY_COLORS.APPLY_IMMEDIATELY},{key:'APPLY_WHEN_OPENING',label:'Prepare to Apply',color:PRIORITY_COLORS.APPLY_WHEN_OPENING},{key:'HIGH_PRIORITY_WATCH',label:'High Priority',color:PRIORITY_COLORS.HIGH_PRIORITY_WATCH},{key:'GOOD_BACKUP',label:'Good Backup',color:PRIORITY_COLORS.GOOD_BACKUP},{key:'LOW_PRIORITY',label:'Low Priority',color:PRIORITY_COLORS.LOW_PRIORITY}]
  const stageCounts=Object.fromEntries(APP_STAGES.map(s=>[s,placements.filter(p=>(p.app_status??'Not Applied')===s).length])) as Record<string,number>; const clearFilters=()=>{setFilterPriority('all');setFilterSector('all');setFilterCountry('all');setFilterStatus('all');setFilterStage('all');setSearch('');setSortBy('relevance')}; const hasFilters=Boolean(search||filterPriority!=='all'||filterSector!=='all'||filterCountry!=='all'||filterStatus!=='all'||filterStage!=='all')
  return <div className="app"><header className="app-header"><div className="header-content"><div className="header-top"><div className="brand"><div className="brand-icon">✈</div><div><h1>2027-28 Placement Tracker</h1><p className="brand-sub">Aerospace · Space · Rockets · F1 · {counts.total} opportunities tracked</p></div></div><div className="header-right"><div className={`connection-indicator ${connected?'connected':'connecting'}`}><span className="conn-dot"/>{connected?'Live':'Connecting…'}</div><button className="download-btn" onClick={()=>downloadExcel(placements)}>Download Excel</button></div></div><div className="stats-row"><div className="stat-chip"><span className="stat-num">{counts.total}</span><span className="stat-label">Total</span></div><button className="stat-chip open" onClick={()=>{setView('opportunities');setFilterStatus('Open Now')}}><span className="stat-num">{counts.open}</span><span className="stat-label">Open Now</span></button><button className="stat-chip soon" onClick={()=>{setView('opportunities');setFilterStatus('Opening Soon')}}><span className="stat-num">{counts.soon}</span><span className="stat-label">Open Soon</span></button><div className="stat-chip expected"><span className="stat-num">{counts.expected}</span><span className="stat-label">Expected</span></div><button className="stat-chip applied" onClick={()=>setView('applications')}><span className="stat-num">{counts.applied}</span><span className="stat-label">My Applications</span></button></div></div></header><main className="app-main"><div className="view-switcher"><button className={view==='opportunities'?'active':''} onClick={()=>setView('opportunities')}>All Opportunities</button><button className={view==='applications'?'active':''} onClick={()=>setView('applications')}>My Applications <span>{counts.applied}</span></button></div>{view==='opportunities'&&<section className="summary-bar">{priorityTabs.map(tab=><button key={tab.key} className={`summary-tab ${filterPriority===tab.key?'active':''}`} onClick={()=>setFilterPriority(tab.key)}><span className="tab-dot" style={{background:tab.color}}/><span className="tab-label">{tab.label}</span><span className="tab-count">{priorityCounts[tab.key]??0}</span></button>)}</section>}{view==='applications'&&<section className="pipeline"><div className="pipeline-title"><h2>Application Pipeline</h2><span>{counts.applied} tracked</span></div><div className="pipeline-stages">{APP_STAGES.map(s=><button key={s} className={filterStage===s?'active':''} onClick={()=>setFilterStage(filterStage===s?'all':s)}><strong>{stageCounts[s]}</strong><span>{s}</span></button>)}</div></section>}<section className="controls"><div className="search-box"><input type="text" placeholder={view==='applications'?'Search applications, notes, contacts…':'Search companies, roles, locations, skills…'} value={search} onChange={e=>setSearch(e.target.value)}/></div><div className="filter-selects"><select value={filterPriority} onChange={e=>setFilterPriority(e.target.value as FilterPriority)}><option value="all">All Priorities</option>{(Object.keys(PRIORITY_LABELS) as OverallPriority[]).map(k=><option key={k} value={k}>{PRIORITY_LABELS[k]}</option>)}</select><select value={filterStatus} onChange={e=>setFilterStatus(e.target.value)}><option value="all">All Statuses</option><option>Open Now</option><option>Opening Soon</option><option>Expected</option><option>Not Yet Published</option><option>Closed</option></select><select value={filterSector} onChange={e=>setFilterSector(e.target.value)}><option value="all">All Sectors</option>{SECTORS.map(s=><option key={s}>{s}</option>)}</select><select value={filterCountry} onChange={e=>setFilterCountry(e.target.value)}><option value="all">All Locations</option>{COUNTRIES.map(c=><option key={c}>{c}</option>)}</select>{view==='applications'&&<select value={filterStage} onChange={e=>setFilterStage(e.target.value)}><option value="all">All My Stages</option>{APP_STAGES.map(s=><option key={s}>{s}</option>)}</select>}<select value={sortBy} onChange={e=>setSortBy(e.target.value as SortOption)}><option value="deadline">Sort: Deadline</option><option value="cv_fit">Sort: CV Fit</option><option value="relevance">Sort: Relevance</option><option value="company">Sort: A–Z</option></select><button className="clear-filters" onClick={clearFilters}>Clear</button></div></section><div className="results-summary"><strong>{filtered.length}</strong> {view==='applications'?'applications':'opportunities'} shown{hasFilters?' with filters':''}</div>{error&&<div className="save-error">{error}</div>}{loading?<div className="state-msg"><p>Loading placements…</p></div>:filtered.length===0?<div className="state-msg"><p>{view==='applications'?'No applications match your filters yet.':'No placements match your filters.'}</p><button onClick={clearFilters}>Clear filters</button></div>:<section className="placements-grid">{filtered.map(p=><PlacementCard key={p.id} placement={p} isNew={newIds.has(p.id)} onUpdate={updatePlacement}/>)}</section>}</main><footer className="app-footer"><span>{filtered.length} {view==='applications'?'application':'placement'}{filtered.length===1?'':'s'}</span><span className="footer-live"><span className="footer-dot"/> Auto-updates when new roles are added or status changes</span></footer></div>
}